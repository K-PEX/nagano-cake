<h3>ジャンル編集</h3>
<%= form_with model: @genre, url: admin_genre_path, local:true do |f| %>
    <div>
      <%= f.label :ジャンル %>
      <%= f.text_field :name %>
      <%= f.submit "変更を保存する" %>
    </div>
<% end %>